-- Reference only: app/db/session.py runs this automatically on startup.
-- Useful if you want to prepare the database by hand (e.g. on a managed
-- Postgres provider before the app has connected once).
CREATE EXTENSION IF NOT EXISTS vector;
